var React = require('react');
var ReactDOM = require('react-dom');

var ClientManagement = React.createClass({
    render:function(){
        return(
            <h2>Client Management Component</h2>
        )
    }
})

module.exports = ClientManagement;
